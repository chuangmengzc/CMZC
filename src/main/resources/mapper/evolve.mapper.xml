<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chuangmeng.cmzc.evolve.dao.EvolveMapper">
    <!--怎加进度-->
    <insert id="addEvolve">
        insert into tb_project_progress(progress_id, project_id, progress_time, progress_info, progress_pic, progress_spare1, progress_spare2) VALUES (
       #{progressId},#{projectId},#{progressTime},#{progressInfo},#{progressPic},#{progressSpare1},#{progressSpare2}
        )
    </insert>
</mapper>
